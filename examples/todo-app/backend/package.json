{
  "name": "todo-backend",
  "version": "0.0.1",
  "private": true,
  "description": "Todo app backend — S-DB-01 migrations package",
  "type": "module",
  "scripts": {
    "migrate:up": "node scripts/migrate.js up",
    "migrate:down": "node scripts/migrate.js down",
    "migrate:status": "node scripts/migrate.js status",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14",
    "vitest": "^2.1.0"
  }
}
