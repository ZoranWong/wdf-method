# Tasks — <PROJECT_NAME>

<!--
  tasks.md is the cross-session continuity载体.
  The Claude session owns this file — wdf only reads it.
  Update it at every step so a fresh session can pick up where you left off.

  Format spec: commands/wdf-tasks.md
-->

Last updated: <ISO timestamp>
Current phase: 1

## In Progress

_None yet — initialize the project with `wdf init` to begin._

## Pending

- [ ] Run `wdf init "<description>"` to create state files
- [ ] Run `wdf start` to begin Phase 1

## Done

- [x] <YYYY-MM-DD> Project scaffolded from `templates/starter/`

## Notes

- <YYYY-MM-DD>: project created from starter template. Override defaults in `wdf.toml`.
