# WDF Method Constitution
> Version: 3.9.0
> Last Updated: 2026-06-22
> Status: ACTIVE

## 1. 项目宗旨

WDF Method (Web Development Flow) 是一个全生命周期Web开发自动化框架。
它将产品需求、设计、架构、开发、测试、部署的完整流程标准化，并通过Agent编排实现自动化执行。

**核心使命：** 让Web开发从"手工工艺"转变为"标准化流水线"。

---

## 2. 不可违背原则

### 2.1 状态一致性原则
> 任何时候都不能有两个真相源

- ✅ 状态目录 `status/` 是唯一真相源
- ✅ `sprint-status.yaml` 只是派生索引，不是主数据
- ✅ 每个状态文件有且仅有一个写入者
- ❌ 禁止绕过状态管理器直接修改状态文件

### 2.2 门禁硬化原则
> 所有规范声明必须是可执行的

- ✅ 未知检查类型 = FAIL，不是PASS
- ✅ 缺少验收条件 = 故事无法启动
- ✅ 命令不在白名单内 = 无法执行
- ✅ 路径遍历检测未通过 = 立即中止
- ❌ 禁止"默认通过"的任何形式

### 2.3 原子性原则
> 操作要么完全成功，要么完全回滚

- ✅ 合并操作：先merge --no-commit → 测试 → 真正commit
- ✅ 状态写入：先备份 → 写入 → 验证
- ✅ Agent调度：全有或全无，无中间状态
- ❌ 禁止部分提交、部分合并

### 2.4 可追溯性原则
> 每个变更都能回溯到其动因

- ✅ 代码变更 → 故事ID → PRD条目 → 用户需求
- ✅ 所有状态变更写入审计日志
- ✅ 每次Agent执行有完整输入输出记录
- ❌ 禁止无故事ID的合并

### 2.5 安全沙箱原则
> Agent永远不能持有破坏系统的能力

- ✅ 所有Shell命令通过白名单验证
- ✅ 工作树隔离，无法访问主目录
- ✅ 禁止sudo、curl、wget等网络/权限命令
- ✅ 所有文件操作限制在项目根目录内

---

## 3. 技术栈约束

### 3.1 执行引擎
- **TypeScript**：所有编排代码
- **Node.js >= 18**：运行时
- **Vitest**：单元测试/集成测试

### 3.2 状态持久化
- **YAML**：人类可读的状态格式
- **JSONL**：审计日志追加写
- **Git**：版本控制

### 3.3 Agent通信
- **文件系统**：状态传递（无IPC/RPC）
- **信号目录**：中断/暂停/恢复
- **stdout/stderr**：仅用于日志，不用于结果解析

---

## 4. 质量红线

### 4.1 测试要求
- 核心编排逻辑测试覆盖率 ≥ 90%
- 每个新特性必须附带：
  - 正向测试（Happy Path）
  - 负向测试（Failure Path）
  - 边界测试（Edge Case）

### 4.2 安全要求
- 所有外部输入必须验证
- 所有路径必须规范化
- Shell参数化执行，禁止字符串拼接
- 白名单机制，禁止黑名单

### 4.3 性能要求
- 状态加载 < 100ms
- 门禁评估 < 50ms
- 测试执行无超时（单测试 < 5s）

---

## 5. 变更治理

### 5.1 宪法修改
本宪章的修改需要：
1. 完整的设计文档说明理由
2. 所有核心测试通过
3. 至少一个完整项目端到端验证通过
4. 语义化版本号主版本+1

### 5.2 规范演进
- 新增检查类型：需要SPEC文档 + 测试用例
- 新增阶段：需要完整的状态机定义 + 回滚路径
- 新增Agent：需要完整的安全边界定义 + 超时机制

---

## 6. 项目边界

### 6.1 WDF Method 负责
- ✅ 流程编排与状态管理
- ✅ 门禁执行与质量保证
- ✅ Agent分发与调度
- ✅ 合并队列与依赖排序
- ✅ 审计与可追溯性

### 6.2 WDF Method 不负责
- ❌ 替代产品经理做决策
- ❌ 替代设计师做设计
- ❌ 替代QA做完整端到端验证（除非明确配置）
- ❌ 替代DevOps做生产部署（除非明确配置）
- ❌ 编写Agent代码本身（Agent由Claude执行）

---

## 7. 版本兼容性

- 同主版本号：向后兼容
- 状态文件格式变更：提供自动迁移脚本
- 配置变更：提供旧配置自动升级
- 无法自动迁移的变更：明确错误信息 + 迁移指南

---

*本宪章是WDF Method开发的最高准则。任何代码、配置、流程违反本宪章的都不应被合并。*