{
  "name": "wdf-method",
  "version": "3.9.0",
  "description": "Web Dev Flow — Full-lifecycle web development automation. PRD through parallel BE/FE implementation.",
  "keywords": ["web-dev", "workflow", "full-stack", "development", "automation", "agents", "bmad"],
  "license": "MIT",
  "bin": {
    "wdf-method": "./tools/installer/wdf-cli.js"
  },
  "files": [
    "tools/installer/wdf-cli.js",
    "SKILL.md",
    "SETUP.md",
    "module.yaml",
    "customize.toml",
    "scripts/setup.sh",
    "skills/",
    "references/",
    "specs/",
    "schemas/",
    "assets/"
  ],
  "scripts": {
    "test": "vitest run && npm --prefix orchestrator run build && npm --prefix orchestrator run test",
    "test:cli": "vitest run",
    "test:orchestrator": "npm --prefix orchestrator run build && npm --prefix orchestrator run test",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.2.0"
  }
}
